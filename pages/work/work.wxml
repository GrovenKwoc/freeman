    <cu-custom bgColor="bg-gradual-blue" isBack="{{true}}">
    <view slot="backText">返回</view>
    <view slot="content">工作</view>
  </cu-custom>
  <!-- 导航 -->
<scroll-view scroll-x class="bg-white nav">
  <view class="flex text-center">
    <view class="cu-item flex-sub {{index==TabCur?'text-orange cur':''}}" wx:for="{{types}}" wx:key bindtap="tabSelect" data-id="{{index}}">
      {{item}}
    </view>
  </view>
</scroll-view>
  <swiper current="{{TabCur}}" duration="300" bindchange="swiperTab" style="height: 50rem;">
    <swiper-item wx:for="{{types}}" wx:key>
      <scroll-view scroll-y="true" bindscrolltolower="loadMore" scroll-top="{{scrollTop}}" bindscrolltoupper="onPullRefresh" style="height:50rem">
        <!-- <view class="cu-card dynamic">
      <view class="cu-item shadow">
        <view class="text-content">
          {{item.Title}}
        </view>
      </view>
    </view> -->
        <view class="cu-card article">
          <view class="cu-item shadow" wx:for="{{DataArr}}" wx:key wx:if="{{index<10}}" bindtap="gotoView" data-url="{{item.Url}}">
            <view class="title">
              <view class="text-cut">{{item.Title}}</view>
            </view>
            <view class="content">
              <!-- <image src="https://ossweb-img.qq.com/images/lol/web201310/skin/big10006.jpg" mode="aspectFill"></image> -->
              <view class="desc">
                <view wx:if="{{item.Desc != ' '}}" class="text-content">{{item.Desc}}</view>
                <view>
                  <view class="cu-tag bg-red light sm round">10W+阅读</view>
                  <view class="cu-tag bg-green light sm round">8166在看</view>
                </view>
              </view>
            </view>
          </view>
        </view>
        <view class="cu-load {{!isLoad?'loading':'over'}}"></view>
      </scroll-view>
    </swiper-item>
  </swiper>